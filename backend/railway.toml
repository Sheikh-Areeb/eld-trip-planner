[build]
builder = "RAILPACK"

[deploy]
startCommand = "python manage.py migrate && uvicorn spotter.asgi:application --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
